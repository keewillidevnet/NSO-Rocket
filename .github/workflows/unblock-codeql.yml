---
name: CodeQL

"on":
  pull_request:
    branches: ["*"]
  workflow_dispatch:

jobs:
  Analyze (python):
    name: Analyze (python)
    runs-on: ubuntu-latest
    steps:
      - name: Satisfy expected "CodeQL / Analyze (python)"
        run: echo "ok"
